# Plan: Initial Local Memory Setup

## Goal

Implement a local Markdown memory system under `dev/` with plans, logs, notes, decisions, handoffs, and a curated index.

## Context

- No existing `AGENTS.md` was found.
- No existing `dev/` memory files were found.
- Repository appears empty at setup time.

## Steps

1. Create `AGENTS.md` with required startup behavior and memory rules.
2. Create `dev/` directories for each memory type.
3. Create `dev/index.md` with only important rediscovery links.
4. Add an ADR-style decision for the memory system.
5. Add a post-work log and a handoff.
6. Verify file layout and git status.
